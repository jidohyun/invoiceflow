package com.invoiceflow.ui.theme

import androidx.compose.ui.unit.dp

// ── Layout ──────────────────────────────────────────────────────
object Tokens {
    // Sidebar
    val SidebarWidth = 256.dp         // 16rem

    // Content
    val ContentMaxWidth = 1280.dp     // 80rem
    val ContentPaddingMobile = 24.dp  // 1.5rem
    val ContentPaddingDesktop = 40.dp // 2.5rem

    // Card
    val CardBorderRadius = 12.dp      // 0.75rem
    val CardBorderWidth = 1.5.dp

    // Sizing
    val BorderWidth = 1.5.dp

    // Spacing scale (8dp base)
    val SpaceXs = 4.dp
    val SpaceSm = 8.dp
    val SpaceMd = 16.dp
    val SpaceLg = 24.dp
    val SpaceXl = 32.dp
    val Space2xl = 40.dp
}
